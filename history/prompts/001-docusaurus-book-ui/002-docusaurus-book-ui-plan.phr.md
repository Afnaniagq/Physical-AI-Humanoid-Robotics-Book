---
id: 2
title: Docusaurus Book UI Plan
stage: plan
date: 2025-12-16T15:30:00Z
surface: cli
model: gemini
feature: 001-docusaurus-book-ui
branch: 001-docusaurus-book-ui
user: unknown
command: "/sp.plan \"Docusaurus Book UI and Navigation ImplementationCreate: architecture sketch, section structure, research approach, quality validation.Decisions needing documentation: list important choices with options and tradeoffs.Testing strategy: validation checks based on acceptance criteria.Technical details:* Use research-concurrent approach (research Docusaurus theming/component creation while implementing the layout).* Follow the project Constitution's \"Platform & Deployment Principles\" (Docusaurus, GitHub Pages).* Organize by phases: Research → Foundation → Implementation → Validation.### Phase 1: Research (20% Time)* **Goal:** Identify the most efficient way to implement the custom two-column layout and dynamic button placement in Docusaurus.* **Research Approach:** Investigate Docusaurus custom pages vs. custom component wrapping for layout injection. Determine the best practice for placing non-content elements (like the Translation Button) within the chapter component without modifying core Docusaurus output.* **Output:** Document selection of Docusaurus Custom Swizzle/Theme Component method.### Phase 2: Foundation (30% Time)* **Goal:** Set up the basic structural components and layout logic.* **Architecture Sketch:**    * **Landing Page:** Use a custom Docusaurus Home Page component. Implement a main $\texttt{<div>}$ with CSS Grid or Flexbox to define the **Left Column** (50% width) and **Right Column** (50% width).    * **Left Column:** Render $\texttt{<BookTitle/>}$, $\texttt{<FeatureButtons/>}$, and navigation links.    * **Right Column:** Render the $\texttt{<ThematicImage/>}$ component. * **Decisions Needing Documentation:**    1. **Layout Method:** CSS Grid vs. Flexbox for two-column split. (Tradeoff: Flexibility vs. Simplicity).    2. **Chapter Navigation:** Standard Docusaurus sidebar vs. embedded $\texttt{Table of Contents}$ component in the Left Column. (Tradeoff: Default behavior vs. Custom Look).### Phase 3: Implementation (40% Time)* **Goal:** Write the necessary React/Docusaurus code and implement the Success Criteria.* **Section Structure:**    * $\texttt{/src/components/ThematicImage.js}$: Component for the right column image.    * $\texttt{/src/theme/CustomLayout.js}$: Component wrapping the default Docusaurus layout to implement the two-column structure for the landing page.    * $\texttt{/src/components/StartCourseButton.js}$: Component for navigation button.    * $\texttt{/src/theme/TOC/index.js}$: Modification or wrapper to move navigation.    * **Button Placeholders:** Implement empty React components for $\texttt{Login/Auth}$ and $\texttt{TranslationToggle}$ within the Left Column and at the top of a sample chapter content page.### Phase 4: Validation (10% Time)* **Goal:** Verify all platform requirements and UI/UX constraints are met.* **Quality Validation (Acceptance Criteria Checklist):**    * [ ] **Two-Column Layout:** Landing page renders correctly with dedicated left/right columns.    * [ ] **Navigation Check:** "Start Course" button successfully links to the **first chapter of Module 1**.    * [ ] **Aesthetic Check:** Styling meets "modern, academic, and clean aesthetic" requirement.    * [ ] **Localization Support:** The dedicated placeholder component for the "Translation Toggle" is correctly positioned at the start of a sample chapter's content area.---